# Base URL of the future Safe Road business backend (e.g. https://api.saferoad.biz).
# Leave empty for now — the frontend runs fully with mocked submissions until this is set.
NEXT_PUBLIC_API_URL=

# Public URL this site is deployed at, used for metadata, canonical links and sitemap.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
